#ifndef LOGAPI_H
#define LOGAPI_H

#include <QObject>
#include "datamanager/datamanager.h"

class logApi : public QObject
{
    Q_OBJECT
public:
    explicit logApi(QObject *parent = 0);
    
signals:

public slots:
    
private:
    dataManager* dataMan;
    QString logDir;
    QFile* logFile;

};

#endif // LOGAPI_H
