#include "logapi.h"

logApi::logApi(QObject *parent) :
    QObject(parent)
{
    dataManager* dataMan=new dataManager();
    logDir=dataMan->getPath()+"app.log";
    QFile* logFile=new QFile(logDir);
    
    
    
}
